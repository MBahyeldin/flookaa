server {
    listen 443 ssl;
    server_name s3.flookaa.com;


    # SSL certificate
    ssl_certificate /etc/letsencrypt/live/s3.flookaa.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/s3.flookaa.com/privkey.pem;

    set $s3_host "{{ containers.s3.ip }}:{{ containers.s3.ports.http }}";

    ## api proxy pass
    location / {
        proxy_pass http://$s3_host;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    ## Health check
    location /health {
        return 200 "OK\n";
    }

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

}
